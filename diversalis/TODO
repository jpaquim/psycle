# This is a sorted list, top priority at the top!

*	#if tests for major.minor.patch version numbers using "cond ? then : else" ternary operator

*	have a look at http://sourceforge.net/projects/predef
