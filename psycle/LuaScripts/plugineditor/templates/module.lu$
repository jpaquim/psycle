-- $(modulename).lua

local $(modulename) = {}

function $(modulename):new()
  local m = {}
  setmetatable(m, self)
  self.__index = self  
  m:init()
  return m
end

function $(modulename):init()
end

return $(modulename)