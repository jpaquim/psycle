# This is a sorted list, top priority at the top!

*	create a wrapper for setenv/SetEnvironmentVariable since putenv has memory policy issues as magnus pointed out.

*	cygwin:
	need to find where the symbols __dso_handle __cxa_atexit are defined,
	and/or why they are needed.
	-- bohan
	
*	microsoft:
	gthread-2.0.lib
	DIVERSALIS__PACKAGENERIC__CONFIGURATION__OPERATING_SYSTEM__VERSION
	universalis::operating_system::logger/terminal sync with mspsyclemfc/legacy/operating_system/logger.{c,h}pp
	-- bohan (sorry, it's cryptic)

*	have a look at gnulib
	-- bohan
